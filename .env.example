# Database (Railway PostgreSQL)
DATABASE_URL="postgresql://user:password@host:5432/railway"

# Gemini API (for audio/video processing)
GEMINI_API_KEY="your-gemini-api-key"

# Anthropic API (for extraction and document generation)
ANTHROPIC_API_KEY="your-anthropic-api-key"

# File Storage (Railway Volume or S3)
STORAGE_TYPE="volume"  # or "s3"
STORAGE_PATH="/data/uploads"  # for volume
# AWS_ACCESS_KEY_ID=""  # for S3
# AWS_SECRET_ACCESS_KEY=""  # for S3
# AWS_S3_BUCKET=""  # for S3
# AWS_REGION=""  # for S3

# App
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NODE_ENV="development"

# ═══════════════════════════════════════════════════════════════════════════════
# AUTHENTICATION
# ═══════════════════════════════════════════════════════════════════════════════

# NextAuth.js secret - generate with: openssl rand -base64 32
AUTH_SECRET="your-auth-secret-generate-with-openssl"

# Single admin user (for now - will expand to database users later)
ADMIN_EMAIL="sophie@freeday.ai"
ADMIN_NAME="Sophie van der Berg"
# Generate hash with: npm run generate-password-hash "your-password"
ADMIN_PASSWORD_HASH="$2a$12$your-bcrypt-hash-here"
